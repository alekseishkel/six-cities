import React from 'react';

import MainPage from '../main-page/main-page.jsx';

const fn = () => {
};

const App = () => {
  return (
    <div>
      <MainPage func={fn} />
    </div>
  );
};

export default App;
